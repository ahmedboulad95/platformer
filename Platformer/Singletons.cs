using System;
using System.Collections.Generic;
using System.Text;
using Microsoft.Xna.Framework;
using Microsoft.Xna.Framework.Graphics;
using Microsoft.Xna.Framework.Input;

namespace Platformer
{
    class Singletons
    {
        /*
        private static TextureAtlas _textureAtlasInstance;
        private static ContentManager _contentManager;

        public TextureAtlas getInstance()
        {
            if(_textureAtlasInstance == null)
            {
                Texture2D worldTextures = Content.Load<Texture2D>("platformerPack_tilesheet");
                Dictionary<string, Vector2> tileCoordinates = new Dictionary<string, Vector2>();
                tileCoordinates.Add("regular_platform", new Vector2(0, 0));
                _textureAtlasInstance = new TextureAtlas(worldTextures, tileCoordinates);
            }
        }
        */
    }
}
